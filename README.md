## Usage

Go to `pumpfun-indexer` root dir in your PC and paste into your terminal

```bash
export RUST_LOG=info
```

then : 
```bash
cargo run
```

and then you will be able to use token querying via endpoint:
```bash
0.0.0.0:5000/tokens
```

